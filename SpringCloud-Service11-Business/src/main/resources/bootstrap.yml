# 從 SpringCloud-Service01-Config 取的配置文件
spring:
  application:
    name: business
  cloud:
    config:
      uri: http://localhost:18990/config
      name: application
      profile: local_biz_config
            
 