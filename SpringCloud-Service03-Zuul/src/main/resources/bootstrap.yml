#取得遠端配置(該配置的順序問題，需要再研究一下)
spring:
  application:
    name: zuul
  cloud:
    config:
      uri: http://localhost:18990/config
      name: application
      profile: local_zuul_config
           
#設定 Eureka 註冊的路徑    
eureka:
    client:
        serviceUrl:
            defaultZone: http://localhost:28990/eureka/

