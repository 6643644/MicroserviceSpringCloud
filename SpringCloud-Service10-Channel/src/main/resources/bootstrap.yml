server:
  port: 38990
spring:
  application:
    name: channel
  cloud:
    config:
      uri: http://localhost:18990/prefix-config
      name: application
      profile: application-local_chl_config

#註冊eureka    
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:28080/eureka/